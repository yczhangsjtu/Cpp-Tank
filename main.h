#ifndef _MAIN_H
#define _MAIN_H

#include <vector>
#include "rect.h"
#include "tank.h"
#include "message.h"
#include "effects.h"

const int minInterval = 10; // Minimal interval between two frames
const int aiInterval = 50; // Make decision every 0.5 seconds
const int addInterval = 10000; // Add a new tank after 10 seconds


///////////////////////////////////////////////////////////////////////////////
// Here are the variables read from file and never changed during the execution


#endif